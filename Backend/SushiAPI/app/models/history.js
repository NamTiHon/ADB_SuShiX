export class History {
    constructor(employeeId, oldBranchId, startDate, endDate) {
        this.employeeId = employeeId;
        this.oldBranchId = oldBranchId;
        this.startDate = startDate;
        this.endDate = endDate;
    }
}
