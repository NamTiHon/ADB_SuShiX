export class OrderDish {
    constructor(orderDishId, orderId, dishId, quantity) {
        this.orderDishId = orderDishId;
        this.orderId = orderId;
        this.dishId = dishId;
        this.quantity = quantity; 
    }
}
